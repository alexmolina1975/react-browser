import styled from 'styled-components';

const StyledParrafo = styled.p `
    font-family: Arial, Helvetica, sans-serif;
    color: white;
    font-weight: bold;
    font-size: 14px;
`;

const StyledPMenu = styled.p `
    font-family: Arial, Helvetica, sans-serif;
    color: white;
    font-weight: bold;
    font-size: 11px;
`;

const StyledParrafoSup = styled.p `
    font-family: Arial, Helvetica, sans-serif;
    color: white;
    font-weight: bold;
    font-size: 80px;
`;

export {StyledParrafo , StyledParrafoSup, StyledPMenu}